# Generated data

The dashboard reads generated JSON datasets from this directory (insights, alpha history, backtests, benchmarks, fundamentals). They are produced locally by the pipeline in `scripts/` and derive from a personal portfolio, so they are not included in the public repo.
